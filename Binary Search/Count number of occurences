class Solution {
    int countFreq(int[] arr, int target) {
        // code here
        int start = countfreq(arr, target);
        int end = countfreq(arr, target);
        if (start == -1 || end == -1) {
            return 0;
        }
        return end - start + 1;
    }
}
